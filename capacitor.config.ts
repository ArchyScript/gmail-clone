import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.archyscriptfirstionicvueproject.app',
  appName: 'Vue Memories',
  webDir: 'public',
  bundledWebRuntime: false,
}

export default config
