import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.archyscriptvacationmemories.app',
  appName: 'Vue Memories',
  webDir: 'public',
  bundledWebRuntime: false,
}

export default config
